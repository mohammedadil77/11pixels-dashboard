import { createTheme } from '@mui/material';

const _11PixelsAppTheme = createTheme({
  palette: {
    primary: {
      main: '#2E1F1F',
    },
    secondary: {
      main: '#f0e5d0',
    },
  },
  typography: {
    fontFamily: ['Poppins'].join(','),
  },
});

export default _11PixelsAppTheme;
